import React, { useState } from "react";
import styles from "../styles/Home.module.css";

const Header = () => {
  return (
    <React.Fragment>
      <div className={styles.header}>けいじばん</div>
    </React.Fragment>
  );
};

export default Header;
